[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kgmcf-reproducibility"
version = "0.3.1"
description = "KGMCF / Aero-MPKG reproducibility package for Ubuntu 22.04 LTS"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
